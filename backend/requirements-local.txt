# FastAPI + ASGI
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# JSON serializer (FastAPI default_response_class=ORJSONResponse requires this)
orjson>=3.10,<4.0

# Pydantic (v2)
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0

# Database (async SQLite)
sqlalchemy>=2.0,<3.0
aiosqlite>=0.20,<1.0

# Auth
python-multipart>=0.0.9,<1.0
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7.4,<2.0

# Logging
structlog>=24.4,<25.0

# ── MaaFramework (core) ──
# 5.12.x stable, ARM64 + AMD64 manylinux wheels on PyPI/piwheels
