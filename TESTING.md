# MAA Web 控制台 — 测试文档

> 随项目进度更新。自动化测试由开发完成，需要人工操作的（真机/真实环境）列在「手动测试清单」。

## 一、自动化测试

> **测试脚本为开发机本机文件，未纳入本仓库**（位于开发机的 `/tmp/opencode/` 目录）。远程/新环境使用本仓库时，需在开发机上按本清单保持测试脚本；命令示例中的路径是开发机路径。

全部测试为 jsdom 或隔离进程运行，**不依赖真实 maa-cli / 设备 / 网络**，可在无设备环境下完整运行。

### 运行方式

```bash
# 开发机执行（脚本位于开发机 /tmp/opencode/）
cd /tmp/opencode && for t in ui-device ui-runner ui-screen ui-queue-checkbox ui-new-fields ui-results ui-schedule ui-queue-v2 ui-poll ui-mxu ui-scrcpy ui-token ui-quick ui-autosave ui-update ui-stages ui-about ui-roguelike ui-copilot ui-configs; do echo "== $t =="; timeout 90 node $t-test.js 2>&1 | tail -1; done && timeout 90 node server-api-test.js 2>&1 | tail -1
```

改动代码后必须全量回归；新增功能必须配套新测试文件并加入上面的清单。

### 测试清单与覆盖

| 测试文件 | 类型 | 覆盖内容 |
|---|---|---|
| `server-api-test.js` | 服务端集成 | 隔离环境（fake maa 二进制 + 独立配置目录）启动真实 server：静态资源、/api/status（版本解析）、task-types/tasks schema、queue CRUD+落盘（enabled 持久化）、队列运行→history 完成、daily.json 生成、update/stop、results 空/404、schedules CRUD+校验、connection 读写、devices 结构、非法参数防御 |
| `ui-device-test.js` | 前端 jsdom | 设备连接页：扫描、设备列表、连接/断开、分辨率加载 |
| `ui-runner-test.js` | 前端 jsdom | 轮询驱动：运行中徽标、输出面板、展开/关闭、失败态（上次失败/error class）、停止按钮显隐 |
| `ui-screen-test.js` | 前端 jsdom | 设备画面弹层：开关、帧率选择、画面拉取（mock 延迟模拟真实网络）、blob 显示、关闭恢复 |
| `ui-queue-checkbox-test.js` | 前端 jsdom | 队列勾选启用/停用、全选/全不选 |
| `ui-new-fields-test.js` | 前端 jsdom | 新增任务表单字段渲染（select/checkbox/chips/multi 等） |
| `ui-results-test.js` | 前端 jsdom | 识别结果页：列表/筛选 chips/公招组合排序/仓库/干员详情/删除/轮询 task:end 联动 |
| `ui-schedule-test.js` | 前端 jsdom | 定时任务页：周几/时间点 chips、开关、编辑、下次执行 |
| `ui-queue-v2-test.js` | 前端 jsdom | 队列拖拽排序（6px 阈值）、右键菜单、状态点 ✓/✗/·、主题切换 |
| `ui-poll-test.js` | 前端 jsdom | 轮询引擎：boot 同步、输出增量、任务开始/结束合成、toast、异常静默 |
| `ui-mxu-test.js` | 前端 jsdom | 日志面板（时间戳/500 行截断/展开/复制/吸底跟随）、强调色（变量/持久化/色块）、任务参数摘要、历史「重新运行」 |

### 状态跟踪

| 模块 | 自动化 | 最近一次 | 备注 |
|---|---|---|---|
| 服务端 API | 32 项 PASS | 2026-08-06 | 全绿 |
| 前端 UI（19 个文件） | 全绿 | 2026-08-06 | 全绿 |
| 识别结果（真实识别） | 部分 | - | 见手动清单 #2 |
| 定时任务（真实触发） | 部分 | - | 调度逻辑已单测，真实触发见手动清单 #6 |
| 设备截图（真实帧率） | 部分 | - | 帧率上限取决于 adb，见手动清单 #3 |
| 多标签页并发 | 无 | - | 见手动清单 #7 |

## 二、手动测试清单（需要真实环境）

以下必须用真实 maa-cli / 模拟器或手机执行，无法自动覆盖：

### 1. 首次启动与环境
- [ ] 全新机器：`bin/maa install` 后各状态页正常显示版本号
- [ ] 设置页：安装/更新 MaaCore、自更新按钮实际执行成功
- [ ] 配置页：读取/新建/编辑/删除 config.toml，保存后文件内容正确

### 2. 工具箱真实识别（需登录游戏）
- [ ] 公开招募：识别结果页出现词条 + 组合表，星级排序正确
- [ ] 仓库识别：材料中文名映射正确（依赖 item_index.json），数量排序正确
- [ ] 干员识别：稀有度分组、潜能/等级显示正确
- [ ] 识别结果历史：切换页面/重启服务后仍可查看，删除结果生效

### 3. 设备画面帧率（真机/模拟器）
- [ ] 无限模式：实际 fps 显示 ~2-4（adb screencap 上限），连续运行 10 分钟无内存增长
- [ ] 5 帧/秒 / 1 帧/秒 / 每 5 秒 / 每 30 秒：切换后间隔符合预期
- [ ] 设备灭屏：显示「画面获取失败」提示，亮屏后自动恢复
- [ ] 画面弹层在手机窄屏下左右有间距（margin 16px）

### 4. 队列运行全流程（真实打游戏）
- [ ] 编排队列 → 运行 → 各任务状态点 ✓/✗ 与实际执行一致（对比 asst.log）
- [ ] 运行中点击停止：任务标记中断（exitCode null）
- [ ] 历史「重新运行」：一键重跑上次队列
- [ ] 队列参数摘要：显示实际生效的参数（非默认值）

### 5. 手机端浏览器（主要使用场景）
- [ ] 深色/浅色/跟随系统主题切换正常
- [ ] 强调色切换：所有 accent 元素（按钮渐变/chip/选中态）跟随
- [ ] 断网 30 秒再恢复：轮询自动续上，日志不重复、任务状态不误报
- [ ] 多标签页同时打开：各自独立操作不串扰（服务端截图缓存生效）

### 6. 定时任务真实触发
- [ ] 设置 1-2 分钟后执行的任务，到点自动运行并在历史出现
- [ ] 服务重启后错过的时间点：3 小时内补执行一次，不重复触发
- [ ] 禁用任务不触发；编辑/删除生效

### 7. 稳定性
- [ ] 持续运行 24 小时：内存无泄漏（日志面板 2000 行上限、轮询间隔稳定）
- [ ] 服务器重启后页面无需手动刷新即自动恢复（轮询兜底）
- [ ] 日志面板上滚暂停跟随、滚回底部恢复；展开 2000 行不卡顿

## 三、已知未自动覆盖项（后续补自动化）

- [ ] 日志按级别过滤（尚未实现，见 ROADMAP.md P3）
- [ ] 队列状态实时自动保存（尚未实现，见 ROADMAP.md P1）
- [ ] 访问令牌鉴权（尚未实现，见 ROADMAP.md P0）
- [ ] server 端 schedules 调度器 tick 补偿逻辑的进程级测试（现有单元测试为内存级）
